* {
  font-family: "Montserrat", sans-serif;
  font-size: 14px;
  /*overflow: hidden;*/
  margin: 0px;
  padding: 0px;
}
.reveal-container {
  position: absolute;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  color: {{fontColor}};
}
.reveal-container h1 {
  text-shadow: 0 0 20px #00000080;
  font-size: 64px;
  text-transform: uppercase;
  white-space: nowrap;
}
.main-container {
  color: {{fontColor}};
  transform: translateY(100%);
  transition: 0.45s ease-in-out;
  position: absolute;
  bottom: 0;
  width: 100%;
  height: 75px;
  background-color: #181919;
  display: flex;
  box-shadow: 10px 5px 50px 0px rgba(0, 0, 0, 0.5);
}
.main-container.active {
  transform: translateY(0%);
}
.main-container .title {
  min-width: 300px;
  height: 100%;
  background-color: {{accentColor}};
  display: flex;
  row-gap: 5px;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}
.main-container .title h1 {
  font-size: 24px;
  text-transform: uppercase;
}
.main-container .title h2 {
  font-size: 16px;
  text-transform: uppercase;
}

.main-container .participants {
  width: calc(100vw - 300px);
  height: 100%;
  overflow: hidden;
  background-color: transparent;
  display: flex;
  column-gap: 15px;
  align-items: center;
  /*   padding-left: 15px; */
  white-space: nowrap;
  flex-direction: row;
}
.main-container .participants .member::not(:last-child) {
  margin-left: 15px;
}
.main-container .participants .member {
  width: fit-content;
  display: inline-block;
  margin: 0 15px;
}
.main-container .participants .member h1 {
  font-size: 24px;
  text-transform: uppercase;
  white-space: nowrap;
}
.main-container .timer {
  position: absolute;
  background-color: #ffffff80;
  bottom: 0;
  left: 300px;
  width: 100%;
  max-width: calc(100vw - 300px);
  height: 5px;
}
